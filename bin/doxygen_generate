#!/bin/sh

if [[ ! -d libjcore ]]; then
	echo "must be run in JX root";
	exit 1;
fi

rm -rf doc/lib*/html;

for d in libj*; do
	if [[ -f $d/Doxyfile ]]; then
		pushd $d;
		doxygen Doxyfile;
		popd;
	fi
done
