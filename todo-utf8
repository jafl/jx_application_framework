JXFileListTable
JXTreeListWidget
JXStringTable
JXStringList
JXDirTable
j2dplot title, labels, legend
	render as many characters as possible via fallback fonts

	JFont:
		flag to allow fallback - makes it a different object!
		if flag is set, break up calls to fontMgr GetCharWidth(), GetStringWidth()
	JXWindowPainter:  base String() checks this flag



JStyledText
	need smarter update during indent/outdent, to maintain caret location
	AutoIndent
	CleanRightMargin

JTextEditor:
	don't change insertion font when glyphs are rendered with different font
		use isExact to tell which glyphs are substitutions?

JXFontManager
	some characters extend above line height

jcc
medic
glove

jx_project_wizard
	convert logic to use JSubstitute - need way to specify case match?
	convert app_template to { }
	convert help to online

code_mill
	not reading from ctags

-----

test
	man pages
	styled unix terminal output - yui build output
	default & vi key handlers
	code_medic on ubuntu with lldb-6.0
	jcc CBExecOutputDocument: test usage of JUtf8ByteBuffer inside JAsynchDataReceiver
	leibnitz - greek characters

test every function that uses JStringIterator or Split
	libjexpr
		?
	libj2dplot
		J2DPlotWidget

unit tests
	JPrefsFile

	JPTPrinter
	JEPSPrinter
	JPSPrinter
	JPSPrinterBase
	JHTMLScanner
	JMDIServer
	JPrefsManager
	JStringManager
	JTEHTMLScanner
	JTEKeyHandler
	JVIKeyHandler
	JVersionSocket
	JWebBrowser

untested JStringIterator usage:
	JVersionSocket
	JWebBrowser::ShowURL
	JWebBrowser::ConvertVarNames
	JPTPrinter::Print
	JPTPrinter::InvertPageOrder
	JPSPrinterBase::PSString
	JGetUserMountPointList
	JTranslateLocalToRemote
	JTranslateRemoteToLocal

-----

https://en.wikibooks.org/wiki/Unicode/Character_reference/21000-21FFF
