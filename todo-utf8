fix jStreamUtil.cpp TODO's
JAsynchDataReceiver::handle_input
	we cannot assume received complete utf-8 characters


finish JTextEditor tests
	GetCmdStatus
	2 TE's for 1 ST
		MouseActions
		KeyPress
		validate caret/selection after TextChanged

JStyledTextBuffer
	need smarter update during indent/outdent, to maintain caret location
	AutoIndent
	CleanRightMargin

JTextEditor:
	undo should select inserted text in the active TextEditor instance
	clicking in selection doesn't set caret location
		treats ending newline line as part of selection
	dropOnSelf crashed
	"Replace All" did nothing when something was selected
	don't change insertion font when glyphs are rendered with different font
		use isExact to tell which glyphs are substitutions?


JXFontManager
	some characters extend above line height

XIM to type utf-8 characters

put back
	JVIKeyHandler
	JTEStyler

implement flex/bison parser for libjexpr

leibnitz - symbol font
	transform to unicode

jx_project_wizard
	convert logic to use JSubstitute - need way to specify case match?
	convert app_template to { }
	convert help to online

-----

test
	man pages
	styled unix terminal output - yui build output
	default & vi key handlers
	code_medic on ubuntu with lldb-6.0

test every function that uses JStringIterator or Split
	libjx
		JXFileListTable
	libjexpr
		?
	libj2dplot
		J2DPlotWidget
	libjfs
		JFSBindingList
		JFSFileTreeNode

unit tests
	JPrefsFile

	JPTPrinter
	JEPSPrinter
	JPSPrinter
	JPSPrinterBase
	JHTMLScanner
	JMDIServer
	JPrefsManager
	JStringManager
	JTEHTMLScanner
	JTEKeyHandler
	JVIKeyHandler
	JVersionSocket
	JWebBrowser

untested JStringIterator usage:
	JExpandHomeDirShortcut - ~user/foo/bar
	JVersionSocket
	JWebBrowser::ShowURL
	JWebBrowser::ConvertVarNames
	JPTPrinter::Print
	JPTPrinter::InvertPageOrder
	JPSPrinterBase::PSString
	JGetUserMountPointList
	JTranslateLocalToRemote
	JTranslateRemoteToLocal

-----

analyze all uses of toupper/lower - not all languages do this by single character

re-evaluate all uses of JIndexRange - character or utf8 byte?

utf8 - sorting vs searching
	searching should include diacriticals
	sorting may not

	test diacriticals in Utf8Char toupper/lower

	ignore accents for case-sensitive search: strength=primary caseLevel=on
	http://demo.icu-project.org/icu-bin/collation.html

-----

https://en.wikibooks.org/wiki/Unicode/Character_reference/21000-21FFF
