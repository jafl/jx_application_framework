choose chinese font based on base font serif
	PingFang, Heiti - sanserif
	Songti - serif
	SC - simplified, TC - traditional, HK - Hong Kong


JTextEditor:
	undo paste doesn't update caret location
	tab makes rest of text invisible
	clicking in selection doesn't set caret location
		treats ending newline line as part of selection
	dropOnSelf crashed

TestTabDirector
	created tabs:  horiz button padding is zero
	&Aring; extends above line height

XIM to type utf-8 characters

finish JTextEditor tests
	GetCmdStatus
	MouseActions
	KeyPress

JStyledTextBuffer
	broadcast char/byte delta with TextChanged, to allow adjusting selection/caret
	AutoIndent
	CleanRightMargin

JTextEditor
	set selection/caret after performing undo operations
+	keep undo active while typing

put back
	JVIKeyHandler
	JTEStyler

implement flex/bison parser for libjexpr

-----

fix TODO's

test
	tip of the day
	man pages
	styled unix terminal output - yui build output
	default & vi key handlers

unit test libjx
	JXCSFDialogBase
	JXDocumentManager
	JXFileListTable
	JXFontManager
	JXHelpManager
	JXHelpText
	JXInputField
	JXMenu
	JXPathInput
	JXSaveFileInput
	JXSpellChecker
	JXStringCompletionMenu
	JXTextButton
	JXTextMenu
	JXTextMenuData
	JXWDManager
	JXWindow
	JXWindowPainter

-----

unit test every function that uses JStringIterator or Split
	libjx
	libjexpr
	libj2dplot
	libjfs

unit tests
	JFileArray
	JPrefsFile

	JPTPrinter
	JEPSPrinter
	JPSPrinter
	JPSPrinterBase
	JHTMLScanner
	JMDIServer
	JPrefsManager
	JStringManager
	JTEHTMLScanner
	JTEKeyHandler
	JTEUndoBase
	JTEUndoStyle
	JTEUndoTabShift
	JTEUndoTextBase
	JVIKeyHandler
	JVersionSocket
	JWebBrowser

untested JStringIterator usage:
	JExpandHomeDirShortcut - ~user/foo/bar
	JVersionSocket
	JWebBrowser::ShowURL
	JWebBrowser::ConvertVarNames
	JPTPrinter::Print
	JPTPrinter::InvertPageOrder
	JPSPrinterBase::PSString
	JGetUserMountPointList
	JTranslateLocalToRemote
	JTranslateRemoteToLocal

JAsynchDataReceiver::handle_input
	we cannot assume received complete utf-8 characters

-----

convert all functions to use JString intead of JUtf8Byte*
	call GetBytes() only when pass string to system function

analyze all uses of toupper/lower - not all languages do this by single character

re-evaluate all uses of JIndexRange - character or utf8 byte?

UTF8_STRING for X selection
use real X compose mechanism
	remove JXComposeRuleList

utf8 - sorting vs searching
	searching should include diacriticals
	sorting may not

	test diacriticals in Utf8Char toupper/lower

	ignore accents for case-sensitive search: strength=primary caseLevel=on
	http://demo.icu-project.org/icu-bin/collation.html

+ replace rendering engine inside JXWindowPainter with UTF-8 version
is YUI yogi output correct with check mark? - NO
	need XDrawString16() ?

leibnitz - symbol font
	transform to unicode and use X11 utf8 string width/drawing functions

----

UText allows regexp to work with utf8
	can ICU regex replace PCRE?  does it use PCRE internally?
ucoleitr, unorm, usearch ought to support UText/UCharIterator
	can it be contributed?

PostScript printing
	ridiculously complicated

-----

https://en.wikibooks.org/wiki/Unicode/Character_reference/21000-21FFF
