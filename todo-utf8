JListIterator:  Next(MatchFunction)



JStyledText
	undo outdent doesn't work correctly when:
		outdent, press return to move to next line, (repeat)
	need smarter update during indent/outdent, to maintain caret location

	AutoIndent
	CleanRightMargin

jcc
	do NOT remove member function data from inheritance trees
		store line number for each function, reported by ctags

	CBCharActionManager only accepts single bytes, not JUtf8Character

-----

render as many characters as possible via fallback fonts
	if (!HasGlyphsForString)
		JXFontManager:
			compute string width in chunks
			compute separate char width if substitution
		JXWindowPainter:
			draw string in chunks

	menu: remove custom font for menu shortcuts

	test with plot labels, testjx file list, file chooser, systemg, menu shortcuts

unit test
	JStyledText - every usage of JRunArrayIterator
	JTextEditor - every usage of JRunArrayIterator
	JTable - especially JRunArrayIterator
	JTableSelection - especially JRunArrayIterator
	JStringTableData
	JFloatTableData
	JPTPrinter - send to file
	JVIKeyHandler:  split into multiple tests covering all options


JTextEditor:
	don't change insertion font when glyphs are rendered with different font
		use isExact to tell which glyphs are substitutions?

jcc
	menu of supported text formats in "open text file" dialog
	sub-menu of supported text formats on File menu, just like "file formats"
medic
	test with JMemoryManager enabled - multiple threads

jx_project_wizard
	convert logic to use JSubstitute - need way to specify case match?
	convert app_template to { }
	convert help to online

glove
	clean up .fd to match current layouts
	test layouts on Linux and OS X

	test column transform:  add two columns

-----

replace JBoolean with bool
	jTypes.h - remove JConvertToBoolean, JI2B, JNegate, operator!, JFloatToBoolean, JF2B

test every usage of JRunArrayIterator
	libjcore
		JSTStyler
	libjx
		JXStringList
		JXStyledText
		JXTextMenuData
	glove
	jcc
	medic

always invalidate JStringIterator if part of a larger function
	glove
	jcc
	medic

JXFontManager
	some characters extend above line height

test
	JVersionSocket - wait for version notice
	man pages
	styled unix terminal output - yui build output
	code_medic on ubuntu with lldb-6.0
	jcc CBExecOutputDocument: test usage of JUtf8ByteBuffer inside JAsynchDataReceiver

test every function that uses JStringIterator, Split
	jcc
		?
	medic
		?
	glove
		FitModule
		GVarList
		GlovePlotter

test JStringIterator usage:
	JTranslateLocalToRemote
	JTranslateRemoteToLocal

-----

https://en.wikibooks.org/wiki/Unicode/Character_reference/21000-21FFF
