# To use this file, first run "makemake" and then run "make".
# (makemake is part of the JX distribution)

JX_ROOT := ../..

JLIBDIR     := ${JX_ROOT}/lib
JCORELIBDIR := ${JX_ROOT}/libjcore
JEXPRLIBDIR := ${JX_ROOT}/libjexpr

# Directories to search for header files

SEARCHDIRS := -I./code \
              -I${JX_ROOT}/include/jcore \
              -I${JX_ROOT}/include/jexpr

# makemake variables

include ${JX_ROOT}/include/make/jx_config
include ${JX_ROOT}/include/make/jexpr_constants

DEPENDFLAGS := ${J_COMPILER_DEPEND_FLAGS} \
               -g ${SEARCHDIRS}

# library dependencies

LIB_DEPS := ${JX_ROOT}/lib/libjexpr.a \
            ${JX_ROOT}/lib/libjx.a \
            ${JX_ROOT}/lib/libjcore.a

# make variables

LOADLIBES := -L${JX_ROOT}/lib -lreflex ${J_IMG_LIBS} ${J_ACE_LIBS} ${J_GCC_LIBS}
LDFLAGS   :=

# tests

TESTS := \
      test_JVariableList test_JUserInputFunction \
      test_JExprParser test_JExprEditor

#####

.PHONY : default
default:
	@cd ..; ${MAKE}

.PHONY : Makefiles
Makefiles:

.PHONY : test
test: ${TESTS}
	$(call run_tests,${TESTS})

.PHONY : touch
touch::
	@cd ..; ${MAKE} TOUCHSTRING=${TOUCHSTRING} touch
