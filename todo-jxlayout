enforce that all selected widgets have the same container
	if not, ignore shift

name of layout must be a valid C class name

align selected: top,bottom,center; right,left,center

shift
	arrow keys
	align to grid - shift key to turn off
	accumulate into single undo
resize
	like window - any edge or corner
	shift key to turn off grid

-----

create widget by cmd-dragging rectangle
	after release, select widget type and then create it - sets correct container

widget options
	label, if appropriate
	shortcuts, if appropriate
	sizing
	variable name, local/instance
	ctor args
	validation (for input fields)
	fit-to-enclosure

generate code when save
	search for file_root .cpp .cc .hpp .h in same directory

	adjust for 1px border when button has ^M shortcut

-----

undo
	create/resize/params/tab-order
	anything that marks document as dirty

allow direct editing of content:
	Toolbar
	ScrollbarSet
	TabGroup
	Partition (set number of compartments & resize them)

	draw grid inside

control tab order
	show index in each item
	cmd-click to select item without re-ordering
	click to make object the next in list
