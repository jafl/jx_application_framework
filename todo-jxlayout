expand scrollbarSet vertically in main layout: too tall
	horizontally becomes too wide

open AboutDialog.jxl with toolbar visible - doesn't compensate correctly
	but it does work when the window is already open

drag image (widget, button, radio) to another layout
	if not in same project (or not saved), can't find images - crash

JXTabGroup
	min 1 value

-----

testjx:
	create layout for TestTabDirector

drakon: tab group in main window

jcc: partitions & tab group
