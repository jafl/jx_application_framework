CREATE TABLE `TIER` (
  TIER_ID bigint NOT NULL AUTO_INCREMENT,
  TIER_NAME varchar(48) NOT NULL,

  CREATE_USER_ID varchar(20) NOT NULL       DEFAULT 'XXX',
  CREATE_DATE datetime NOT NULL             DEFAULT CURRENT_TIMESTAMP,
  UPDATE_USER_ID varchar(20) NOT NULL       DEFAULT 'XXX',
  UPDATE_DATE datetime NOT NULL             DEFAULT CURRENT_TIMESTAMP /* ON UPDATE CURRENT_TIMESTAMP */,
  PRIMARY KEY (TIER_ID),
  UNIQUE KEY (TIER_NAME)
);

-- comment

INSERT INTO `TIER` (TIER_ID, TIER_NAME) VALUES
(1, 'TIER_1'),
(2, 'TIER_2'),
(3, "TIER_3");

# MySQL comment
