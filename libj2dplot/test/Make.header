# To use this file, first run "makemake" and then run "make".
# (makemake is part of the JX distribution)

JX_ROOT := ../..

# Directories to search for header files

SEARCHDIRS := -I./code \
              -I${JX_ROOT}/include/jcore \
              -I${JX_ROOT}/include/jx \
              -I${JX_ROOT}/include/jexpr \
              -I${JX_ROOT}/include/j2dplot

# makemake variables

J_STRING_FILES := strings/*

include ${JX_ROOT}/include/make/jx_config
include ${JX_ROOT}/include/make/jexpr_constants
include ${JX_ROOT}/include/make/j2dplot_constants

DEPENDFLAGS := ${J_COMPILER_DEPEND_FLAGS} \
               -g ${SEARCHDIRS}

# library dependencies

LIB_DEPS := ${JX_ROOT}/lib/libj2dplot.a \
            ${JX_ROOT}/lib/libjexpr.a \
            ${JX_ROOT}/lib/libjx.a \
            ${JX_ROOT}/lib/libjcore.a

# make variables

LOADLIBES := -L${JX_ROOT}/lib -lreflex ${J_ACE_LIBS} ${J_GCC_LIBS}
LDFLAGS   := 

# tests

TESTS := \
      test_J2DPlotWidget

#####

.PHONY : default
default:
	@cd ..; ${MAKE}

.PHONY : Makefiles
Makefiles:

.PHONY : test
test: ${TESTS}
	$(call run_tests,${TESTS})

.PHONY : touch
touch::
	@cd ..; ${MAKE} TOUCHSTRING=${TOUCHSTRING} touch
